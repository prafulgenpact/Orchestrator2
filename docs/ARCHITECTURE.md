# Architecture

<!-- Keep current — the auditor and every new joiner reads this. Update in the
     same PR as any structural change (the constitution requires it). -->

## System overview

One diagram (ASCII or link) + one paragraph: the components and how data flows.

## Components

For each component: responsibility, entry point file, key dependencies, owner.

| Component | Responsibility | Entry point | Depends on |
|---|---|---|---|
| … | … | `src/…` | … |

## Key flows

Walk the 2-3 flows that explain 80% of the system, file by file.

## External services & integrations

Service, purpose, auth method, failure behavior, where configured.

## Decisions

Significant choices live in docs/decisions/ (ADRs). Index the important ones here.
