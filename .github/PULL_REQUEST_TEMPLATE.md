## Task

Link: `specs/tasks/<id>.md` — Phase: <from specs/plan.md>

## What changed and why (2-4 sentences)

## Evidence (the contract — no checkbox, no merge)

- [ ] `make verify` PASS locally; proof sealed (`proofs/latest.json` in this PR)
- [ ] Unit tests: <n> passed, coverage <x>% (from proof)
- [ ] E2E tests: <n> passed (from proof)
- [ ] All changes within task `Scope:` (drift check clean)
- [ ] Bugfix only: regression test added that fails without the fix
- [ ] Docs/STATE.md updated (or n/a because …)

## Auditor verdict (paste the block from `make audit` / `@auditor`)

```
VERDICT: …
```
