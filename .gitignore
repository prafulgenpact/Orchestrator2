# caches & environments
__pycache__/
*.pyc
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
node_modules/
dist/
build/

# transient verification workspace (proofs/ IS committed; reports/ is not)
reports/
.verify.lock/
.scaffold-backup/

# session handoff notes + rendered chart output (both are scratch, not deliverables)
RESUME.md
orchestrator-output/

# saved run history / observability sinks (local run data, not deliverables)
runs/
logs/
observability.db

# secrets & local overrides
.env
.env.*
.claude/settings.local.json
