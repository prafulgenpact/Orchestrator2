# Pinned verification toolchain — determinism requires exact versions.
# Update deliberately (one PR, all environments), never ad hoc.
ruff==0.5.5
mypy==1.11.1
pytest==8.3.2
pytest-cov==5.0.0
pip-audit==2.7.3
diff_cover==9.1.1

# Runtime dependencies. The project is run via PYTHONPATH (never pip-installed), so they
# are pinned here to be installed in dev + CI. anthropic is imported only in live/record mode;
# httpx is used by the app-caller/executor to invoke the apps over HTTP.
anthropic==0.109.1
httpx==0.28.1
# websockets powers the live-kernel transport (coding-playground run_code over WS).
websockets==12.0
